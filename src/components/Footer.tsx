import React, { FC } from 'react';

const Footer: FC = () => {
  return (
    <div className="flex justify-between items-center h-[50px] px-5 shadow-md bg-gradient-to-r from-sky-500 to-indigo-500 text-white">
    </div>
  );
};

export default Footer;
